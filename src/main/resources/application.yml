spring:
  application:
    name: switchyard
  datasource:
    url: jdbc:postgresql://localhost:5455/switchyard
    username: switchyard
    password: switchyard_dev_only
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
